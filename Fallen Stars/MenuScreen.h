#pragma once

#include <SFML/Graphics/RenderTarget.hpp>
#include <vector>

#include "Controls.h"
#include "MenuItem.h"
/*
class MenuScreen
{
	public:
		virtual ~Menu();
		virtual void update(float deltaTime) = 0;
		virtual void render(sf::RenderTarget& renderSurface) = 0;
		virtual void handleAction(Controls::Action action, Controls::KeyState) = 0;
		virtual void mouseMoved(int x, int y, int state) = 0; // In case we implement mouse input

	private:
		std::vector<MenuItem*>
};
*/